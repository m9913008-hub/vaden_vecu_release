# VADEN VECU release bundle

離線更新包：內含 `INSTALL.sh` 與 `camera_<YYYYMMDD>_<build>.tar.zst`。

## 安裝（目標機）

```bash
sudo -E ./INSTALL.sh ./camera_<YYYYMMDD>_<build>.tar.zst
```

詳見專案內 `INSTALL.sh` 檔頭註解與壓縮檔頂層之 `install.manifest`。
